<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container">
    <a class="navbar-brand" href="/">
      <span style="font-size: 1.2em;">💎</span> SubTracker
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (locals.currentUser) { %>
          <li class="nav-item me-2">
            <div class="theme-dropdown">
              <button class="theme-toggle" title="Changer le thème">
                <span class="icon">☀️</span>
              </button>
              <div class="theme-dropdown-menu">
                <button class="theme-option" data-theme="light">
                  <span>☀️</span>
                  <span>Clair</span>
                  <span class="check">✓</span>
                </button>
                <button class="theme-option" data-theme="dark">
                  <span>🌙</span>
                  <span>Sombre</span>
                  <span class="check">✓</span>
                </button>
                <button class="theme-option" data-theme="auto">
                  <span>🌓</span>
                  <span>Auto</span>
                  <span class="check">✓</span>
                </button>
              </div>
            </div>
          </li>
          <li class="nav-item">
            <span class="nav-link text-muted fw-bold">Bonjour, <%= currentUser.username %></span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/subscriptions">Dashboard</a>
          </li>
          <li class="nav-item ms-2">
            <button class="btn btn-outline-danger btn-sm" data-bs-toggle="modal" data-bs-target="#logoutModal">
              Déconnexion
            </button>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
<script src="/js/theme.js"></script>

<% if (locals.currentUser) { %>
<div class="modal fade" id="logoutModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Confirmation</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Voulez-vous vraiment vous déconnecter ?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
        <a href="/auth/logout" class="btn btn-danger">Oui, me déconnecter</a>
      </div>
    </div>
  </div>
</div>
<% } %>